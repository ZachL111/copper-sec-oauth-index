# Field Notes

`copper-sec-oauth-index` is easiest to review by starting with the fixture, not the prose.

The domain cases cover `trust boundary`, `claim drift`, `replay exposure`, and `policy width`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

`recovery` is the strongest case at 265 on `policy width`. `baseline` is the cautious anchor at 126 on `trust boundary`.

The local verifier covers this data so the notes stay tied to code.
